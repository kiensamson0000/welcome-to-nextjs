export const metadata = {
  title: "View Detail Blog",
  description: "CHI TIET BLOG",
};

export default function ViewDetailLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
